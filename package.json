{
  "name": "redux-ittt",
  "version": "0.0.3",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "test:watch": "jest --watchAll",
    "prepublish": "yarn run build",
    "postpublish": "rm -rf dist",
    "prebuild": "rm -rf dist",
    "build": "rollup ./src/index --format umd --output dist/index.js --name 'redux-ittt'"
  },
  "peerDependencies": {
    "redux": "^3.7.2"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "jest": "^21.0.1",
    "redux-mock-store": "^1.2.3"
  }
}
